// This file will run before each test file
import { beforeAll, afterAll, beforeEach } from 'vitest';

beforeAll(() => {
  // Global setup like starting MSW worker if needed
});

afterAll(() => {
  // Global teardown
});

beforeEach(() => {
  // Clear state between tests
});
