const user: { name: string; age: number; active: boolean } = {
  name: "John",
  age: 30,
  active: true
};

const profile: {
  name: string;
  age?: number;
  active?: boolean;
} = {
  name: "divya"
};

console.log(profile)